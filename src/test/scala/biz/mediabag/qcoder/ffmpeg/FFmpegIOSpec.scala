package biz.mediabag.qcoder.ffmpeg

import org.scalatest.FunSuite

class FFmpegIOSuite extends FunSuite {
  test("Container factory registers codecs without exception") (pending)

  test("Container factory loads file and returns correct streams") (pending)  
  
}